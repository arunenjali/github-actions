name: actions-workflow
on: push

jobs:
  job1:
    name: job1
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        run: |
          pwd
          ls -ltr
          hostname
      - name: step 2 - checkout the code
        uses: actions/checkout@v6.0.2
      - name: step 3
        run: |
          pwd
          ls -ltr
          hostname

        
